min = length(data);
if min > length(data_int)
    min = length(data_int);
end
if min > length(data_int)
    min = length(data_int);
end
if min > length(data_int)
    min = length(data_int);
end
if min > length(data_int)
    min = length(data_int);
end

A = [data data_obs_int]

table = array2table(data,"VariableNames",["reference","systeme reel"]);


writetable(table,'donees.csv');